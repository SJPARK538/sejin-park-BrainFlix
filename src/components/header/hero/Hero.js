import React from "react";
import "./hero.scss"

const Hero = () => {
    return(
        <div class="hero">
            <div class="hero__container"></div>
        </div>
    )
}

export default Hero;